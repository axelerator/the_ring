#include "vertexarray.h"
#include <GL/gl.h>

class Primitive

GLenum pType;
unsigned int vCount;
unsigned int used;

constructor(GLenum pType, unsigned int vCount)
: vCount(vCount), pType(pType), used(0)
  ;

public unsigned int next()
  return (used++);

